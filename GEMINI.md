# GEMINI Agent Guidelines

> **CRITICAL INSTRUCTION**: Auto-Deploy Enabled

To ensure the live website is always up-to-date, obey the following rule:

**AFTER ANY CODE MODIFICATION:**
1.  **Commit** changes immediately.
2.  **Push** to `main` branch.

Command Reference:
```bash
git add .
git commit -m "update: applied changes by gemini"
git push
```
